# Earth Rover Vehicle Control Station - Environment Variables Configuration
# Copy this file to .env and update with your actual values
# DO NOT commit the .env file with actual credentials/IPs to version control

# =============================================================================
# ROS Connection Configuration
# =============================================================================

# ROS Bridge WebSocket URL
# This is the address where the rosbridge_server is running
# Default: ws://192.168.1.7:9090
ROS_BRIDGE_URL=ws://192.168.1.7:9090

# Video Server URL
# This is the address where the web_video_server is running
# Default: http://192.168.1.7:8080
VIDEO_SERVER_URL=http://192.168.1.7:8080

# =============================================================================
# Trike Stack / MAVROS (used by scripts/startup/run_trike_stack.sh)
# =============================================================================
# GCS URL for QGroundControl / Mission Planner (UDP). Passed to MAVROS as gcs_url.
# Set empty to omit: GCS_URL=
GCS_URL=udp://192.168.1.7:14550

# Pixhawk serial (optional; run_trike_stack.sh defaults if unset)
# PIXHAWK_DEVICE=/dev/serial/by-id/usb-FTDI_TTL232R-3V3_FTD16B5P-if00-port0
# PIXHAWK_BAUD=921600

# =============================================================================
# Usage Instructions
# =============================================================================
# 
# 1. Copy this file to create your own .env file:
#    cp env.example .env
#
# 2. Edit .env with your vehicle's IP address
#
# 3. If running everything on the same machine, you can use:
#    ROS_BRIDGE_URL=ws://localhost:9090
#    VIDEO_SERVER_URL=http://localhost:8080
#
# 4. For remote vehicle access, replace with vehicle's IP:
#    ROS_BRIDGE_URL=ws://YOUR_VEHICLE_IP:9090
#    VIDEO_SERVER_URL=http://YOUR_VEHICLE_IP:8080
#
# 5. To load environment variables in Django:
#    - Install python-dotenv: pip install python-dotenv
#    - Add to manage.py or settings.py:
#      from dotenv import load_dotenv
#      load_dotenv()
#
# 6. To set environment variables in terminal:
#    export ROS_BRIDGE_URL=ws://192.168.0.35:9090
#    export VIDEO_SERVER_URL=http://192.168.0.35:8080
#
# 7. run_trike_stack.sh loads vehicle_control_station/.env automatically (GCS_URL, etc.).
#
# 8. To set environment variables in startup script:
#    Add these lines before starting Django:
#    export ROS_BRIDGE_URL=ws://192.168.0.35:9090
#    export VIDEO_SERVER_URL=http://192.168.0.35:8080
#
# =============================================================================
# Common IP Configurations
# =============================================================================
#
# Local development (all services on same machine):
#   ROS_BRIDGE_URL=ws://localhost:9090
#   VIDEO_SERVER_URL=http://localhost:8080
#
# Vehicle on local network:
#   ROS_BRIDGE_URL=ws://192.168.0.35:9090
#   VIDEO_SERVER_URL=http://192.168.0.35:8080
#
# Vehicle with custom IP:
#   ROS_BRIDGE_URL=ws://10.0.0.100:9090
#   VIDEO_SERVER_URL=http://10.0.0.100:8080
#

